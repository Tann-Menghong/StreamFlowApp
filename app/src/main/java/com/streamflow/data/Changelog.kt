package com.streamflow.data

// User-facing release notes shown in the "What's New" dialog after an update.
// MUST be updated on every version bump so users see what changed.
object Changelog {
    const val VERSION_NAME = "5.1.0"

    val notes = listOf(
        "NEW MKissa tab 🎭 — mkissa.to dramas & movies with full ad blocking, popunder protection and resume-where-you-left-off",
        "Tab settings redesigned ✨ — Settings > Home is now organized into clean groups: Layout, Bottom navigation, and Start & defaults",
        "All five optional tabs (Search, Donghua, Drama, PDTV, MKissa) now live in one tidy \"Bottom navigation\" card with clear descriptions",
        "Bottom bar label style moved next to the tab switches where it belongs",
        "MKissa can be your start screen too (Settings > Home > Start screen)"
    )
}
